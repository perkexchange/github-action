# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the main branch
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "reward"
  reward:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Rewards the initiating user with cryptocurrency       
      - uses: perkexchange/github-action@v0.3
        name: Perk.Exchange Reward
        with:
            apiToken: 88290042b76cf82cffa292233439df17749a1d3c6d0cbb372fe3c1f73120e539
            campaignReference: c19d6d5ba87925cc3f9526f6bba442bf04c5c807569e5dbc8f344e44323b7066
            userId: ${{ github.event.sender.id }}
            platform: github
            amount: 3
            server: https://9c7d-99-251-23-45.ngrok.io
